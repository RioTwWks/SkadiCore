# cargo-deny — https://embarkstudios.github.io/cargo-deny/
# Supply-chain policy: advisories, banned TLS stacks, trusted sources.

[graph]
all-features = false
no-default-features = false

[advisories]
version = 2
db-urls = ["https://github.com/rustsec/advisory-db"]
yanked = "deny"
unmaintained = "workspace"
unsound = "all"
ignore = [
    # Transitive via rustls dev-deps in tests; skadi crates use rustls-pki-types::PemObject.
    { id = "RUSTSEC-2025-0134", reason = "rustls-pemfile only via rustls test dependency" },
]

[licenses]
version = 2
confidence-threshold = 0.8
allow = [
    "0BSD",
    "AGPL-3.0",
    "AGPL-3.0-or-later",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "MIT",
    "MPL-2.0",
    "OpenSSL",
    "Unicode-3.0",
    "WTFPL",
    "Zlib",
]

[bans]
multiple-versions = "warn"
wildcards = "allow"
# SkadiCore uses rustls + ring; OpenSSL / native-tls stacks are forbidden.
deny = [
    { name = "openssl" },
    { name = "openssl-sys" },
    { name = "native-tls" },
    { name = "tokio-native-tls" },
    { name = "hyper-tls" },
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
