# SkadiCore — TODO

Ядро для обхода блокировок на Rust. Гибрид идей Xray (gRPC API, REALITY)
и sing-box (низкое потребление, TUN, множество протоколов).

Лицензия: AGPL-3.0-or-later
Статус: MVP в разработке

---

## 🎯 Философия проекта

- **Безопасность прежде всего.** Никакой своей криптографии — только
  проверенные крейты (`rustls`, `ring`, `aes-gcm`).
- **Модульность.** Каждый компонент — отдельный крейт с чётким API.
- **Zero-copy где возможно.** Минимум аллокаций на горячем пути.
- **Один бинарник.** Ядро должно собираться в один статический бинарник
  без внешних зависимостей (musl target).
- **Наблюдаемость.** Структурные логи, метрики, трейсинг с первого дня.

---

## 📐 Этап 0: Фундамент (1 неделя)

- [ ] Инициализировать workspace с крейтами:
  - [ ] `skadi-core` — трейты, типы, ошибки
  - [ ] `skadi-transport` — TCP, TLS, будущие транспорты
  - [ ] `skadi-protocol` — SOCKS5, VLESS, будущие протоколы
  - [ ] `skadi-server` — бинарник
  - [ ] `skadi-config` — парсинг TOML/JSON, валидация
- [ ] Настроить CI (GitHub Actions):
  - [ ] `cargo fmt --check`
  - [ ] `cargo clippy -- -D warnings`
  - [ ] `cargo test`
  - [ ] `cargo audit` (проверка CVE в зависимостях)
- [ ] Настроить `tracing` + `tracing-subscriber` с JSON-форматом
- [ ] Базовые типы: `UserId`, `Session`, `Endpoint`, `Protocol`
- [ ] Иерархия ошибок через `thiserror`, контекст через `anyhow`

**Критерий готовности:** `cargo run` запускается, пишет структурированный
лог, CI зелёный.

---

## 🔌 Этап 1: TCP-прокси (1 неделя)

- [ ] Реализовать `TcpListener` с graceful shutdown
- [ ] Обработка каждого соединения в отдельной задаче tokio
- [ ] `copy_bidirectional` между inbound и outbound
- [ ] Таймауты на:
  - [ ] установку соединения (connect timeout)
  - [ ] неактивность (idle timeout)
  - [ ] общую длительность сессии (max session lifetime)
- [ ] Лимит одновременных соединений (backpressure)
- [ ] Корректное закрытие: полузакрытые соединения, RST, FIN
- [ ] Юнит-тесты: соединиться, передать данные, закрыть

**Критерий готовности:** можно использовать как простой TCP-форвардер.
Работает под нагрузкой `wrk`/`iperf3` без утечек.

---

## 🧦 Этап 2: SOCKS5 (1–2 недели)

- [ ] Парсинг handshake:
  - [ ] greeting (версия, методы аутентификации)
  - [ ] выбор метода (no-auth / user-pass)
  - [ ] запрос (CMD, ATYP, ADDR, PORT)
- [ ] Поддержка команд:
  - [ ] CONNECT
  - [ ] BIND (опционально, позже)
  - [ ] UDP ASSOCIATE (опционально, позже)
- [ ] Аутентификация по логину/паролю (RFC 1929)
- [ ] Reply-коды (успех, ошибки, отказ)
- [ ] Fuzz-тестирование парсера (`cargo-fuzz`)
- [ ] Интеграционные тесты с реальным клиентом (`curl --socks5`)

**Критерий готовности:** `curl --socks5 127.0.0.1:1080 https://example.com`
работает. Парсер не падает на битых пакетах.

**⚠️ Риск:** это первый код, обрабатывающий недоверенные данные.
Обязательно fuzz-тесты и строгие лимиты на длины полей.

---

## 🔐 Этап 3: TLS-транспорт (1 неделя)

- [x] Интегрировать `rustls` + `tokio-rustls`
- [x] Загрузка сертификатов (PEM) из конфига
- [x] SNI-роутинг (несколько сертификатов на одном порту)
- [x] ALPN (h2, http/1.1)
- [x] Настройка cipher suites (только AEAD, только TLS 1.3)
- [x] Интеграционный тест SOCKS5 over TLS
- [ ] Тесты с `openssl s_client` (ручная проверка)

**Критерий готовности:** TCP-прокси работает поверх TLS. Клиент
подключается, проверяет сертификат, получает данные.

**⚠️ Риск:** ошибки в проверке сертификата = MITM. Использовать только
`rustls`, не `native-tls`, и не отключать проверку ни при каких условиях.

---

## 📡 Этап 4: VLESS (2–3 недели)

- [ ] Парсинг заголовка VLESS:
  - [ ] version (1 байт)
  - [ ] UUID (16 байт)
  - [ ] addons length + addons
  - [ ] command (TCP/UDP/Mux)
  - [ ] port (2 байта)
  - [ ] address type (IPv4/IPv6/domain)
  - [ ] address
- [ ] Response-заголовок
- [ ] Хранилище пользователей: UUID → настройки (in-memory + reload)
- [ ] Проверка UUID за постоянное время (`subtle::ConstantTimeEq`)
- [ ] Обработка неизвестных UUID (fallback, не падать)
- [ ] Поддержка UDP over VLESS (опционально)
- [ ] Fuzz-тесты парсера

**Критерий готовности:** клиент v2rayNG/Nekoray подключается к SkadiCore
через VLESS и работает HTTP.

**⚠️ Риск:** timing-атаки на UUID, утечки через различающееся поведение.
Все проверки — константное время.

---

## 🎭 Этап 5: REALITY (3–4 недели, самый сложный)

- [ ] Изучить `rustls-reality` — форк `rustls` с поддержкой REALITY
- [ ] Интеграция REALITY в TLS-слой:
  - [ ] Эфемерный X25519 key exchange
  - [ ] Подпись временного ключа приватным ключом сервера
  - [ ] Проверка подписи на клиенте
  - [ ] Маскировка под указанный `serverName` (SNI)
- [ ] Fallback на реальный сайт при неверном handshake
  - [ ] Проксирование TLS-трафика на `dest` (например, `vkvideo.ru`)
  - [ ] Прозрачная передача без расшифровки
- [ ] Настройка `shortIds`, `serverNames`, `dest`
- [ ] Тесты против активного зондирования:
  - [ ] `openssl s_client` видит настоящий сертификат `dest`
  - [ ] Тайминги handshake не отличаются от настоящего сайта
  - [ ] Отпечаток JA3/JA4 совпадает с браузерным

**Критерий готовности:** активный зонд не может отличить SkadiCore от
настоящего `vkvideo.ru`. Клиент с REALITY подключается и работает.

**⚠️ Риск:** это самая критичная часть. Ошибка = раскрытие сервера.
Не писать с нуля — использовать `rustls-reality` и тщательно тестировать.

---

## 🔄 Этап 6: gRPC API (2 недели)

Это то, чего нет у sing-box и что делает Xray пригодным для панелей.

- [ ] Определить protobuf-схему:
  - [ ] `HandlerService.AddUser(user)`
  - [ ] `HandlerService.RemoveUser(user)`
  - [ ] `HandlerService.ListUsers()`
  - [ ] `StatsService.GetStats(user_id)`
  - [ ] `StatsService.GetOnlineUsers()`
- [ ] Генерация кода через `tonic`
- [ ] In-memory хранилище с `RwLock` (или `dashmap` для параллелизма)
- [ ] Горячее добавление/удаление пользователей **без перезагрузки ядра**
- [ ] Защита API:
  - [ ] Слушать только на `127.0.0.1` по умолчанию
  - [ ] Опциональный TLS + токен-аутентификация
  - [ ] Rate limiting
- [ ] Метрики: активные соединения, байты ↑/↓, ошибки

**Критерий готовности:** внешняя панель (3X-UI, Marzban) может управлять
пользователями через gRPC API.

**⚠️ Риск:** ошибка в API = компрометация всей системы. Обязательно
изоляция, аутентификация, аудит.

---

## 📊 Этап 7: Наблюдаемость (параллельно всем этапам)

- [ ] `tracing` с JSON-логами
- [ ] Prometheus-метрики через `metrics` + `metrics-exporter-prometheus`:
  - [ ] `skadi_connections_total`
  - [ ] `skadi_connections_active`
  - [ ] `skadi_bytes_in_total` / `skadi_bytes_out_total`
  - [ ] `skadi_handshake_duration_seconds` (гистограмма)
  - [ ] `skadi_errors_total{kind}`
- [ ] Health-check endpoint (`/healthz`)
- [ ] Опциональный OpenTelemetry экспорт

---

## 🖥️ Этап 8: Конфигурация и CLI (параллельно)

- [ ] TOML-схема конфига с `serde`:
  ```toml
  [server]
  listen = "0.0.0.0:443"

  [protocol.vless]
  enabled = true

  [transport.reality]
  enabled = true
  dest = "vkvideo.ru:443"
  server_names = ["vkvideo.ru"]
  private_key = "..."

  [api]
  listen = "127.0.0.1:8080"
  token = "..."
  ```
- [ ] Валидация конфига с понятными ошибками
- [ ] `clap` для CLI: `--config`, `--log-level`, `--check-config`
- [ ] SIGHUP для reload конфига без перезапуска
- [ ] Генерация ключей (`skadicore genkey reality`)

---

## 🧪 Этап 9: Тестирование и безопасность

- [ ] Юнит-тесты: покрытие ≥ 70% для парсеров
- [ ] Интеграционные тесты: поднятие сервера + клиента в одном процессе
- [ ] Fuzz-тесты (`cargo-fuzz`) для всех парсеров
- [ ] `cargo miri` для проверки UB в unsafe-коде
- [ ] Бенчмарки (`criterion`): пропускная способность, latency, CPU
- [ ] Нагрузочные тесты: 10k одновременных соединений
- [ ] Аудит зависимостей (`cargo audit`, `cargo deny`)
- [ ] Проверка на утечки:
  - [ ] DNS-утечки
  - [ ] IPv6-утечки
  - [ ] Утечки памяти под нагрузкой (24ч soak test)

---

## 📦 Этап 10: Релиз и распространение

- [ ] Кросс-компиляция:
  - [ ] `x86_64-unknown-linux-musl` (основной)
  - [ ] `aarch64-unknown-linux-musl` (ARM-серверы, роутеры)
  - [ ] `x86_64-pc-windows-msvc`
  - [ ] `aarch64-apple-darwin`
- [ ] Docker-образ (scratch + статический бинарник)
- [ ] GitHub Releases с автогенерацией
- [ ] Документация:
  - [ ] `README.md` с быстрым стартом
  - [ ] `docs/ARCHITECTURE.md`
  - [ ] `docs/PROTOCOLS.md`
  - [ ] Примеры конфигов для типовых сценариев
- [ ] Страница донатов (GitHub Sponsors, Boosty)

---

## 🚫 Что НЕ делать (антипаттерны)

- ❌ **Своя криптография.** Только `rustls`, `ring`, `aes-gcm`, `chacha20poly1305`.
- ❌ **Свой TLS с нуля для REALITY.** Использовать `rustls-reality`.
- ❌ **`unwrap()` / `expect()` на горячем пути.** Только обработка ошибок.
- ❌ **`unsafe` без крайней необходимости.** Если нужен — `// SAFETY:` комментарий.
- ❌ **Логи с паролями, UUID, ключами.** Только идентификаторы сессий.
- ❌ **Отключение проверки TLS.** Даже для тестов — использовать self-signed CA.
- ❌ **Блокирующие вызовы в async-контексте.** `spawn_blocking` для CPU-heavy.

---

## 📅 Ориентировочный график

| Этап | Длительность | Зависит от |
|---|---|---|
| 0. Фундамент | 1 нед | — |
| 1. TCP-прокси | 1 нед | 0 |
| 2. SOCKS5 | 1–2 нед | 1 |
| 3. TLS | 1 нед | 1 |
| 4. VLESS | 2–3 нед | 2, 3 |
| 5. REALITY | 3–4 нед | 4 |
| 6. gRPC API | 2 нед | 4 |
| 7. Наблюдаемость | параллельно | 0 |
| 8. Конфиг/CLI | параллельно | 0 |
| 9. Тесты/безопасность | параллельно | все |
| 10. Релиз | 1 нед | все |

**Итого до первого стабильного релиза: ~4–6 месяцев** при работе
в свободное время. MVP (этапы 0–2) — ~1 месяц.

---

## 🎯 Definition of Done для MVP

- [ ] Один статический бинарник
- [ ] TCP-прокси + SOCKS5 работает
- [ ] Конфиг через TOML
- [ ] Структурные логи
- [ ] Тесты покрывают парсеры
- [ ] CI зелёный
- [ ] README с быстрым стартом
- [ ] Лицензия AGPL-3.0-or-later в репозитории

---

## 📚 Референсы для изучения

- **Xray-core** (Go) — архитектура, gRPC API, REALITY
- **sing-box** (Go) — TUN, минимализм, поддержка протоколов
- **xray-lite** (Rust) — VLESS + REALITY + eBPF на Rust
- **rustls-reality** (Rust) — REALITY для rustls
- **fast-socks5** (Rust) — зрелая реализация SOCKS5
- **Tokio** — документация и примеры
- **Cloudflare Pingora** (Rust) — zero-copy проксирование
